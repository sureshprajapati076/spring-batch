#file: noinspection undefined
spring:
  datasource:
    username: batch
    password: batch
    url: jdbc:mysql://localhost:3306/person
query: select * from users where id in ('2','4')  # result of this query will be selected for processing
---
spring:
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

# spring.batch.initialize-schema=always
